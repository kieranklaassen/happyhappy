<%# Plaintext for these fixtures is "Secret-Passw0rd" (>= MINIMUM_PASSWORD_LENGTH). %>
<% password_digest = BCrypt::Password.create("Secret-Passw0rd") %>

one:
  email_address: one@example.com
  password_digest: <%= password_digest %>

two:
  email_address: two@example.com
  password_digest: <%= password_digest %>

every_ana:
  email_address: ana@every.to
  every_user_id: every-user-ana
  name: Ana Every
  avatar_url: https://every.to/avatars/ana.png
